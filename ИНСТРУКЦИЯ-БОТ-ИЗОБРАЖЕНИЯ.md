# Инструкция: Настройка бота для обработки изображений

Чтобы бот мог отправлять изображения в мини-приложение для анализа, нужно настроить обработчик фото в коде бота.

---

## Вариант 1: Передача URL изображения (РЕКОМЕНДУЕТСЯ)

Бот получает URL изображения и передает его в мини-приложение через `start_param`.

### Пример кода для бота (Python, python-telegram-bot)

```python
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup, WebAppInfo
from telegram.ext import ContextTypes
import urllib.parse

async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик фотографий от пользователя"""
    # Получаем самое большое фото
    photo = update.message.photo[-1]
    
    # Получаем file_id
    file_id = photo.file_id
    
    # Получаем URL файла через Bot API
    file = await context.bot.get_file(file_id)
    file_url = f"https://api.telegram.org/file/bot{context.bot.token}/{file.file_path}"
    
    # Кодируем URL для передачи в start_param
    encoded_url = urllib.parse.quote(file_url, safe='')
    
    # Формируем start_param
    start_param = f"img_url_{encoded_url}"
    
    # URL твоего мини-приложения
    web_app_url = "https://your-app.netlify.app"  # Замени на свой URL
    
    # Создаем кнопку для открытия мини-приложения
    keyboard = InlineKeyboardMarkup([[
        InlineKeyboardButton(
            "📸 Анализировать изображение",
            web_app=WebAppInfo(url=f"{web_app_url}?startapp={start_param}")
        )
    ]])
    
    await update.message.reply_text(
        "Открываю мини-приложение для анализа изображения...",
        reply_markup=keyboard
    )

# Регистрируем обработчик
application.add_handler(MessageHandler(filters.PHOTO, handle_photo))
```

---

## Вариант 2: Передача file_id (требует bot_token в приложении)

Бот передает только `file_id`, а мини-приложение само получает файл через Bot API.

### Пример кода для бота

```python
async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    photo = update.message.photo[-1]
    file_id = photo.file_id
    
    # Просто передаем file_id
    start_param = f"img_{file_id}"
    
    web_app_url = "https://your-app.netlify.app"
    
    keyboard = InlineKeyboardMarkup([[
        InlineKeyboardButton(
            "📸 Анализировать изображение",
            web_app=WebAppInfo(url=f"{web_app_url}?startapp={start_param}")
        )
    ]])
    
    await update.message.reply_text(
        "Открываю мини-приложение для анализа изображения...",
        reply_markup=keyboard
    )
```

**Важно:** Для этого варианта нужно добавить `bot_token` в мини-приложение:
```javascript
sessionStorage.setItem('bot_token', 'YOUR_BOT_TOKEN');
```

---

## Вариант 3: Автоматическое открытие (без кнопки)

Если хочешь, чтобы мини-приложение открывалось автоматически:

```python
async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    photo = update.message.photo[-1]
    file = await context.bot.get_file(photo.file_id)
    file_url = f"https://api.telegram.org/file/bot{context.bot.token}/{file.file_path}"
    encoded_url = urllib.parse.quote(file_url, safe='')
    start_param = f"img_url_{encoded_url}"
    
    web_app_url = "https://your-app.netlify.app"
    
    # Отправляем сообщение с кнопкой, которая автоматически откроет мини-приложение
    await update.message.reply_text(
        "Анализирую изображение...",
        reply_markup=InlineKeyboardMarkup([[
            InlineKeyboardButton(
                "Открыть",
                web_app=WebAppInfo(url=f"{web_app_url}?startapp={start_param}")
            )
        ]])
    )
```

---

## Полный пример бота (Python)

```python
import logging
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup, WebAppInfo
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes
import urllib.parse

# Включи логирование
logging.basicConfig(format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', level=logging.INFO)

# Твой токен бота
BOT_TOKEN = "YOUR_BOT_TOKEN"
# URL твоего мини-приложения
WEB_APP_URL = "https://your-app.netlify.app"

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "Привет! Отправь мне фотографию товара, и я помогу создать карточку желания."
    )

async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик фотографий"""
    try:
        # Получаем самое большое фото
        photo = update.message.photo[-1]
        file_id = photo.file_id
        
        # Получаем URL файла
        file = await context.bot.get_file(file_id)
        file_url = f"https://api.telegram.org/file/bot{context.bot.token}/{file.file_path}"
        
        # Кодируем URL
        encoded_url = urllib.parse.quote(file_url, safe='')
        start_param = f"img_url_{encoded_url}"
        
        # Создаем кнопку
        keyboard = InlineKeyboardMarkup([[
            InlineKeyboardButton(
                "📸 Анализировать изображение",
                web_app=WebAppInfo(url=f"{WEB_APP_URL}?startapp={start_param}")
            )
        ]])
        
        await update.message.reply_text(
            "Открываю мини-приложение для анализа изображения...",
            reply_markup=keyboard
        )
    except Exception as e:
        logging.error(f"Error handling photo: {e}")
        await update.message.reply_text("Произошла ошибка. Попробуй еще раз.")

def main():
    # Создаем приложение
    application = Application.builder().token(BOT_TOKEN).build()
    
    # Регистрируем обработчики
    application.add_handler(CommandHandler("start", start))
    application.add_handler(MessageHandler(filters.PHOTO, handle_photo))
    
    # Запускаем бота
    application.run_polling(allowed_updates=Update.ALL_TYPES)

if __name__ == '__main__':
    main()
```

---

## Проверка работы

1. Отправь боту фотографию товара
2. Бот должен ответить сообщением с кнопкой "Анализировать изображение"
3. Нажми на кнопку — должно открыться мини-приложение
4. В мини-приложении должно появиться сообщение "Анализирую изображение..."
5. Через 10-20 секунд должна открыться форма с автозаполненными данными

---

## Отладка

Если ничего не происходит:

1. **Проверь логи бота** — есть ли ошибки при обработке фото?
2. **Проверь URL мини-приложения** — правильный ли он в коде бота?
3. **Проверь start_param** — правильно ли он формируется?
4. **Открой консоль браузера** (F12) в мини-приложении — есть ли ошибки?
5. **Проверь, что start_param начинается с `img_`** — мини-приложение ищет именно такой формат

### Проверка start_param в мини-приложении

Открой консоль браузера (F12) и выполни:
```javascript
console.log('Start param:', window.Telegram?.WebApp?.initDataUnsafe?.start_param);
```

Должно быть что-то вроде: `img_url_https%3A%2F%2Fapi.telegram.org%2Ffile%2F...`

---

## Готово!

После настройки бота функция анализа изображений должна работать. Если возникнут проблемы — проверь логи и консоль браузера.
