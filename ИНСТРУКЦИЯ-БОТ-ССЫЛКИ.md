# Инструкция: Настройка бота для анализа ссылок

Чтобы бот обрабатывал ссылки на товары и открывал мини-приложение для создания карточки желания по анализу страницы.

---

## Алгоритм

1. Пользователь отправляет боту ссылку в чат
2. Бот показывает кнопку «🔗 Анализировать ссылку»
3. Пользователь нажимает кнопку
4. Открывается мини-приложение с формой создания желания
5. Поля заполняются автоматически (название, цена, размер из страницы товара)
6. Пользователь может править, отменить или сохранить

---

## Код для бота (Python, python-telegram-bot)

```python
import re
import urllib.parse
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup, WebAppInfo
from telegram.ext import ContextTypes

WEB_APP_URL = "https://your-app.pages.dev"  # Замени на свой URL

def is_url(text):
    return bool(re.match(r'^https?://\S+$', (text or '').strip()))

async def handle_link(update: Update, context: ContextTypes.DEFAULT_TYPE):
    text = (update.message.text or '').strip()
    if not is_url(text):
        await update.message.reply_text(
            "Отправь ссылку на товар (https://...), и я помогу создать карточку желания."
        )
        return

    encoded_url = urllib.parse.quote(text, safe='')
    start_param = f"link_{encoded_url}"
    app_url = f"{WEB_APP_URL}#tgWebAppStartParam={urllib.parse.quote(start_param, safe='')}"

    keyboard = InlineKeyboardMarkup([[
        InlineKeyboardButton(
            "🔗 Анализировать ссылку",
            web_app=WebAppInfo(url=app_url)
        )
    ]])

    await update.message.reply_text(
        "Нажми кнопку ниже, чтобы открыть мини-приложение и создать карточку желания по ссылке.",
        reply_markup=keyboard
    )

# Регистрация обработчика (должен идти до общего handle_all)
application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_link))
```

---

## Вариант для telebot (pyTelegramBotAPI)

```python
import re
import urllib.parse
from telebot import types

WEB_APP_URL = "https://your-app.pages.dev"  # Замени на свой URL

def is_url(text):
    return bool(re.match(r'^https?://\S+$', (text or '').strip()))

@bot.message_handler(func=lambda m: is_url(m.text))
def handle_link(message):
    text = message.text.strip()
    encoded_url = urllib.parse.quote(text, safe='')
    start_param = f"link_{encoded_url}"
    app_url = f"{WEB_APP_URL}#tgWebAppStartParam={urllib.parse.quote(start_param, safe='')}"

    keyboard = types.InlineKeyboardMarkup()
    keyboard.add(types.InlineKeyboardButton(
        "🔗 Анализировать ссылку",
        web_app=types.WebAppInfo(url=app_url)
    ))

    bot.reply_to(message,
        "Нажми кнопку ниже, чтобы открыть мини-приложение и создать карточку желания по ссылке.",
        reply_markup=keyboard
    )
```

---

## Порядок обработчиков

Если у тебя уже есть обработчик текста (например, для фото), убедись, что обработчик ссылок проверяется **до** общего fallback:

```python
# Сначала — ссылки
application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_link))

# Потом — фото
application.add_handler(MessageHandler(filters.PHOTO, handle_photo))

# В конце — общий fallback
application.add_handler(MessageHandler(filters.TEXT, handle_all))
```

---

## Настройка Cloudflare

Для работы анализа ссылок в Cloudflare Pages добавь переменные окружения:

1. **Cloudflare Dashboard** → твой проект → **Settings** → **Environment variables**
2. Добавь одну из:
   - `OPENROUTER_API_KEY` — ключ OpenRouter (бесплатные модели)
   - `GROQ_API_KEY` — ключ Groq (если предпочитаешь Groq)

Без ключа будет использоваться только парсинг мета-тегов (og:*, JSON-LD). С ключом — дополнительно LLM для страниц без структурированных данных.

---

## Проверка

1. Отправь боту ссылку на товар (например, Wildberries, Ozon, любой интернет-магазин)
2. Должна появиться кнопка «🔗 Анализировать ссылку»
3. Нажми — откроется мини-приложение
4. Должны подставиться название, цена, размер (если есть на странице)
5. Можно редактировать и сохранить карточку желания

---

## Формат start_param

Мини-приложение ожидает `start_param` вида: `link_<encoded_url>`

Пример: для `https://example.com/product/123`  
start_param = `link_https%3A%2F%2Fexample.com%2Fproduct%2F123`
