# Простая инструкция: Как настроить бота для обработки фото

## Где находится код бота?

Бот работает на сервере (не в Telegram). Код бота может быть:

1. **На твоём компьютере** (если запускаешь бота локально)
2. **На хостинге** (например, Heroku, Railway, VPS сервер)
3. **В облачном сервисе** (например, PythonAnywhere, Replit)

---

## Как найти код бота?

### Вариант 1: Если бот на Python (самый частый случай)

Ищи файл с названием типа:
- `bot.py`
- `main.py`
- `app.py`
- `handler.py`
- Или любой `.py` файл, где есть `BOT_TOKEN` или `application.run_polling()`

### Вариант 2: Если бот на Node.js (JavaScript)

Ищи файл:
- `bot.js`
- `index.js`
- `app.js`
- Или любой `.js` файл с `bot.token` или `bot.launch()`

### Вариант 3: Если используешь конструктор ботов (без кода)

Если ты создавал бота через конструктор (типа ManyChat, Chatfuel), то нужно использовать другой подход — через вебхуки или интеграции.

---

## Что делать дальше?

### Шаг 1: Найди файл с кодом бота

Открой папку, где лежит код бота, и найди главный файл (обычно `bot.py` или `main.py`).

### Шаг 2: Открой файл в редакторе

Можно использовать:
- **Блокнот** (Windows)
- **VS Code** (рекомендуется)
- **PyCharm** (если Python)
- Любой текстовый редактор

### Шаг 3: Найди место, где обрабатываются сообщения

Ищи в коде что-то вроде:
```python
# Python примеры
@bot.message_handler(content_types=['photo'])
def handle_photo(message):
    ...

# Или
async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    ...
```

Или:
```javascript
// JavaScript примеры
bot.on('photo', (msg) => {
    ...
});
```

### Шаг 4: Добавь код обработки фото

**Если у тебя УЖЕ ЕСТЬ обработчик фото:**
- Замени его кодом из инструкции `ИНСТРУКЦИЯ-БОТ-ИЗОБРАЖЕНИЯ.md`

**Если обработчика фото НЕТ:**
- Добавь новый обработчик в конец файла (перед `bot.polling()` или `application.run_polling()`)

---

## Пример: Куда вставить код

### Если у тебя простой бот на Python (pyTelegramBotAPI):

**Было:**
```python
import telebot

bot = telebot.TeleBot("YOUR_BOT_TOKEN")

@bot.message_handler(commands=['start'])
def start(message):
    bot.reply_to(message, "Привет!")

bot.polling()
```

**Стало:**
```python
import telebot
from telebot import types
import urllib.parse

bot = telebot.TeleBot("YOUR_BOT_TOKEN")

WEB_APP_URL = "https://your-app.netlify.app"  # ← ЗАМЕНИ НА СВОЙ URL!

@bot.message_handler(commands=['start'])
def start(message):
    bot.reply_to(message, "Привет!")

# ← ДОБАВЬ ЭТОТ КОД:
@bot.message_handler(content_types=['photo'])
def handle_photo(message):
    try:
        # Получаем самое большое фото
        photo = message.photo[-1]
        file_id = photo.file_id
        
        # Получаем информацию о файле
        file_info = bot.get_file(file_id)
        file_url = f"https://api.telegram.org/file/bot{bot.token}/{file_info.file_path}"
        
        # Кодируем URL
        encoded_url = urllib.parse.quote(file_url, safe='')
        start_param = f"img_url_{encoded_url}"
        
        # Создаем кнопку
        keyboard = types.InlineKeyboardMarkup()
        button = types.InlineKeyboardButton(
            text="📸 Анализировать изображение",
            web_app=types.WebAppInfo(url=f"{WEB_APP_URL}?startapp={start_param}")
        )
        keyboard.add(button)
        
        bot.reply_to(message, "Открываю мини-приложение для анализа изображения...", reply_markup=keyboard)
    except Exception as e:
        bot.reply_to(message, f"Ошибка: {e}")

bot.polling()
```

---

## Важно: Замени URL!

В коде есть строка:
```python
WEB_APP_URL = "https://your-app.netlify.app"
```

**Замени `your-app.netlify.app` на реальный URL твоего мини-приложения!**

Например, если твой сайт: `https://wishlist-app-123.netlify.app`, то:
```python
WEB_APP_URL = "https://wishlist-app-123.netlify.app"
```

---

## После изменений

1. **Сохрани файл** (Ctrl+S)
2. **Перезапусти бота:**
   - Если бот запущен на компьютере — останови (Ctrl+C) и запусти заново
   - Если бот на хостинге — перезапусти через панель управления или перезагрузи сервер

---

## Как проверить, что работает?

1. Отправь боту фотографию товара
2. Бот должен ответить сообщением с кнопкой "📸 Анализировать изображение"
3. Нажми на кнопку
4. Должно открыться мини-приложение
5. Должно появиться сообщение "Анализирую изображение..."

---

## Если ничего не работает

### Проверь:

1. **Правильный ли URL мини-приложения?**
   - Открой URL в браузере — должен открыться сайт
   
2. **Перезапущен ли бот после изменений?**
   - Бот не подхватывает изменения автоматически, нужен перезапуск

3. **Есть ли ошибки в логах бота?**
   - Посмотри вывод бота в консоли — там могут быть ошибки

4. **Правильно ли скопирован код?**
   - Проверь, что нет опечаток, все кавычки закрыты

---

## Нужна помощь?

Если не получается:
1. Скажи, на каком языке написан бот (Python/JavaScript)
2. Скажи, где он запущен (компьютер/хостинг)
3. Пришли фрагмент кода бота (можно без токена!)

И я помогу настроить конкретно под твой случай!
