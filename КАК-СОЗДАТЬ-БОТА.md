# Как создать бота для обработки фото

## Что у тебя есть сейчас?

✅ **Мини-приложение** (frontend) — готово, работает  
✅ **Бот зарегистрирован** в BotFather — есть токен  
❌ **Код бота** (backend) — нужно создать

---

## Что такое код бота?

Бот — это программа, которая:
- Слушает сообщения в Telegram
- Отвечает на команды (например, `/start`)
- Обрабатывает фото и открывает мини-приложение

**Сейчас у тебя:** Бот зарегистрирован, но не умеет ничего делать, потому что нет кода.

---

## Варианты создания бота (от простого к сложному)

### Вариант 1: Использовать готовый сервис (САМЫЙ ПРОСТОЙ) 🎯

Есть сервисы, где можно создать бота без программирования:

#### A. **ManyChat** или **Chatfuel**
- Визуальный конструктор ботов
- Можно настроить обработку фото
- **Минус:** Может быть платным и ограниченным

#### B. **Glitch.com** (бесплатно, но требует базовые знания)
- Готовые шаблоны ботов
- Можно скопировать код и запустить

---

### Вариант 2: Простой Python бот (РЕКОМЕНДУЕТСЯ) 🐍

Если у тебя есть компьютер с Python, это самый простой способ.

#### Шаг 1: Установи Python

1. Скачай с https://www.python.org/downloads/
2. При установке отметь "Add Python to PATH"
3. Проверь: открой командную строку и введи `python --version`

#### Шаг 2: Установи библиотеку для бота

Открой командную строку и введи:
```bash
pip install pytelegrambotapi
```

#### Шаг 3: Создай файл `bot.py`

Создай файл `bot.py` в любой папке и скопируй туда:

```python
import telebot
from telebot import types
import urllib.parse

# Твой токен бота (из BotFather)
BOT_TOKEN = "ТВОЙ_ТОКЕН_ОТСЮДА"

# URL твоего мини-приложения (замени на свой!)
WEB_APP_URL = "https://your-app.netlify.app"

bot = telebot.TeleBot(BOT_TOKEN)

@bot.message_handler(commands=['start'])
def start(message):
    bot.reply_to(message, "Привет! Отправь мне фотографию товара, и я помогу создать карточку желания.")

@bot.message_handler(content_types=['photo'])
def handle_photo(message):
    try:
        # Получаем самое большое фото
        photo = message.photo[-1]
        file_id = photo.file_id
        
        # Получаем информацию о файле
        file_info = bot.get_file(file_id)
        file_url = f"https://api.telegram.org/file/bot{bot.token}/{file_info.file_path}"
        
        # Кодируем URL для передачи в мини-приложение
        encoded_url = urllib.parse.quote(file_url, safe='')
        start_param = f"img_url_{encoded_url}"
        
        # Создаем кнопку для открытия мини-приложения
        keyboard = types.InlineKeyboardMarkup()
        button = types.InlineKeyboardButton(
            text="📸 Анализировать изображение",
            web_app=types.WebAppInfo(url=f"{WEB_APP_URL}?startapp={start_param}")
        )
        keyboard.add(button)
        
        bot.reply_to(message, "Открываю мини-приложение для анализа изображения...", reply_markup=keyboard)
    except Exception as e:
        bot.reply_to(message, f"Произошла ошибка: {e}")

# Запускаем бота
print("Бот запущен! Нажми Ctrl+C для остановки.")
bot.polling()
```

#### Шаг 4: Замени данные

1. **Замени `ТВОЙ_ТОКЕН_ОТСЮДА`** на токен из BotFather
2. **Замени `your-app.netlify.app`** на реальный URL твоего мини-приложения

#### Шаг 5: Запусти бота

Открой командную строку в папке с файлом `bot.py` и введи:
```bash
python bot.py
```

Бот запустится и будет работать, пока не закроешь окно.

**⚠️ Проблема:** Бот работает только пока открыт компьютер. Для постоянной работы нужен хостинг.

---

### Вариант 3: Хостинг для бота (чтобы работал 24/7) ☁️

#### A. **Railway.app** (бесплатно, просто)

1. Зарегистрируйся на https://railway.app
2. Подключи GitHub репозиторий с кодом бота
3. Railway автоматически запустит бота

#### B. **PythonAnywhere** (бесплатно, но ограничено)

1. Зарегистрируйся на https://www.pythonanywhere.com
2. Загрузи файл `bot.py`
3. Запусти через консоль

#### C. **Heroku** (бесплатно, но сложнее)

Требует больше настроек, но надежный.

---

## Что нужно сделать СЕЙЧАС?

### Минимальный вариант (для теста):

1. **Создай файл `bot.py`** на компьютере (код выше)
2. **Установи Python и библиотеку** (инструкция выше)
3. **Замени токен и URL** в коде
4. **Запусти бота** через командную строку
5. **Отправь фото боту** — должно работать!

### Для постоянной работы:

После теста на компьютере — залей бота на хостинг (Railway или PythonAnywhere).

---

## Как узнать токен бота?

1. Открой Telegram
2. Найди бота **@BotFather**
3. Отправь команду `/mybots`
4. Выбери своего бота
5. Нажми **API Token**
6. Скопируй токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## Как узнать URL мини-приложения?

Это URL твоего сайта на Netlify. Например:
- `https://wishlist-app-123.netlify.app`
- Или любой другой URL, который дал Netlify

---

## Проверка работы

1. Запусти бота (`python bot.py`)
2. Открой Telegram
3. Найди своего бота
4. Отправь команду `/start` — бот должен ответить
5. Отправь фото — бот должен ответить с кнопкой
6. Нажми кнопку — должно открыться мини-приложение

---

## Нужна помощь?

Если что-то не получается:
1. Скажи, на каком шаге застрял
2. Пришли текст ошибки (если есть)
3. Скажи, установлен ли Python

Помогу разобраться!
