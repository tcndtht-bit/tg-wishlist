# Инструкция: Настройка API для анализа изображений

Эта инструкция поможет тебе настроить API для функции автоматического создания карточек желаний из фотографий.

## 🆓 Бесплатные варианты (рекомендуется начать с них!)

### ⭐ Вариант 1: Tesseract.js (РАБОТАЕТ В БЕЛАРУСИ И РОССИИ!)
- ✅ **Полностью бесплатно** и без ограничений
- ✅ **Работает локально в браузере** - не требует API ключей
- ✅ **Доступен везде** - не блокируется в Беларуси/России
- ✅ Не требует регистрации
- ⚠️ Чуть медленнее (10-20 секунд на анализ)
- 📖 [Инструкция ниже](#вариант-1-tesseractjs-работает-в-беларуси)

### Вариант 2: Google Gemini API
- ✅ **Полностью бесплатно** до 15 запросов в минуту
- ✅ Отличное качество распознавания
- ⚠️ Может быть заблокирован в Беларуси/России
- 📖 [Инструкция ниже](#вариант-2-google-gemini-api)

### Вариант 3: OCRAPI.cloud
- ✅ **250 запросов бесплатно** в месяц
- ✅ Простая настройка
- ⚠️ Меньше точность, чем у Gemini
- 📖 [Инструкция ниже](#вариант-3-ocrapicloud)

### Вариант 4: OpenAI (платный)
- ⚠️ Платно (~$0.01-0.03 за изображение)
- ✅ Лучшее качество
- 📖 [Инструкция ниже](#вариант-4-openai-платный)

---

## ⭐ Вариант 1: Tesseract.js (РАБОТАЕТ В БЕЛАРУСИ!) 🆓

### Что это?

Tesseract.js — это библиотека OCR, которая работает **полностью в браузере**. Она не требует API ключей, не отправляет данные на серверы и работает даже без интернета (после первой загрузки).

### Преимущества:

- ✅ **Работает везде** — не блокируется в Беларуси, России и других странах
- ✅ **Полностью бесплатно** — без ограничений и лимитов
- ✅ **Приватность** — изображения не покидают твой компьютер
- ✅ **Не требует регистрации** — работает сразу
- ✅ **Поддерживает русский язык**

### Недостатки:

- ⚠️ Медленнее облачных API (10-20 секунд на анализ)
- ⚠️ Меньше точность на сложных изображениях

### Настройка

**Ничего настраивать не нужно!** Tesseract.js уже подключен в приложении и работает автоматически.

Просто используй функцию анализа изображений — она сама выберет Tesseract.js как первый вариант.

### Как это работает

1. При первом использовании Tesseract.js загрузит языковые модели (русский + английский) — это займёт несколько секунд
2. Затем изображение анализируется прямо в браузере
3. Весь процесс занимает 10-20 секунд (зависит от размера изображения)
4. Результаты автоматически заполняются в форму

### Готово! 🎉

Tesseract.js работает сразу, без ключей и регистрации. Идеально для Беларуси и России!

**Совет:** Для лучших результатов используй чёткие фотографии с хорошим освещением и читаемым текстом.

---

## Вариант 2: Google Gemini API 🆓

### Шаг 1. Получить API ключ Gemini

1. Открой: **https://aistudio.google.com/app/apikey**
2. Войди через Google аккаунт
3. Нажми **Create API Key**
4. Выбери **Create API key in new project** (или существующий проект)
5. **Скопируй ключ** — он выглядит так: `AIzaSy...`
6. Сохрани ключ в надёжное место

✅ **Готово!** Ключ бесплатный, не требует карты, работает сразу.

### Шаг 2. Настроить в приложении

**Способ A: В коде (простой)**
1. Открой `index.html`
2. Найди строку: `const GEMINI_API_KEY = '';`
3. Вставь ключ: `const GEMINI_API_KEY = 'AIzaSy...';`

**Способ B: Через браузер (безопаснее)**
1. Открой приложение
2. Нажми F12 (консоль разработчика)
3. Введи:
   ```javascript
   sessionStorage.setItem('gemini_key', 'AIzaSy...');
   ```
4. Обнови страницу (F5)

### Готово! 🎉

Теперь функция будет использовать Gemini API бесплатно.

---

## Вариант 3: OCRAPI.cloud 🆓

### Шаг 1. Получить API ключ

1. Открой: **https://ocr.space/ocrapi**
2. Нажми **Sign Up** (регистрация)
3. Зарегистрируйся через email
4. Подтверди email
5. После входа скопируй **API Key** из личного кабинета

✅ **250 запросов бесплатно** каждый месяц!

### Шаг 2. Настроить в приложении

**В коде:**
```javascript
const OCRAPI_KEY = 'твой-ключ-отсюда';
```

**Или через браузер:**
```javascript
sessionStorage.setItem('ocrapi_key', 'твой-ключ');
```

---

## Вариант 4: OpenAI (платный) 💰

---

## Часть 1: Регистрация и получение API ключа (OpenAI)

### Шаг 1.1. Создать аккаунт OpenAI

1. Открой в браузере: **https://platform.openai.com**
2. Нажми **Sign up** (Зарегистрироваться)
3. Зарегистрируйся через:
   - Email и пароль
   - Google аккаунт
   - Microsoft аккаунт
4. Подтверди email, если попросят

---

### Шаг 1.2. Пополнить баланс (обязательно!)

⚠️ **Важно:** OpenAI API платный. Нужно пополнить баланс перед использованием.

1. После входа нажми на свой профиль (правый верхний угол)
2. Выбери **Billing** (или **Usage**)
3. Нажми **Add payment method** (Добавить способ оплаты)
4. Введи данные карты (Visa, Mastercard и т.д.)
5. Пополни баланс минимум на **$5-10** (этого хватит на много запросов)

**Стоимость:** Анализ одного изображения стоит примерно **$0.01-0.03** (зависит от размера изображения).

---

### Шаг 1.3. Создать API ключ

1. В левом меню нажми **API keys** (или перейди по ссылке: https://platform.openai.com/api-keys)
2. Нажми **Create new secret key**
3. Придумай название (например, "Wishlist App")
4. Нажми **Create secret key**
5. **ВАЖНО:** Скопируй ключ сразу! Он показывается только один раз.
   - Ключ выглядит так: `sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
   - Сохрани его в надёжное место (записная книжка, менеджер паролей)

---

## Часть 2: Настройка API ключа в приложении

### Какой API использовать?

Приложение автоматически пробует API в таком порядке:
1. **Gemini** (если указан ключ)
2. **OpenAI** (если указан ключ)
3. **OCRAPI** (если указан ключ)

Можно изменить приоритет в коде:
```javascript
const CV_API_PRIORITY = ['gemini', 'ocrapi']; // сначала пробует Gemini, потом OCRAPI
```

### Способ 1: Прямо в коде

Открой `index.html` и укажи ключ для нужного API:

```javascript
// Для Gemini (бесплатно)
const GEMINI_API_KEY = 'AIzaSy...';

// Или для OCRAPI (бесплатно)
const OCRAPI_KEY = 'твой-ключ';

// Или для OpenAI (платно)
const OPENAI_API_KEY = 'sk-proj-...';
```

### Способ 2: Через браузер (безопаснее)

1. Открой приложение
2. Нажми **F12** (консоль разработчика)
3. Введи команду для нужного API:

**Для Gemini:**
```javascript
sessionStorage.setItem('gemini_key', 'AIzaSy...');
```

**Для OCRAPI:**
```javascript
sessionStorage.setItem('ocrapi_key', 'твой-ключ');
```

**Для OpenAI:**
```javascript
sessionStorage.setItem('openai_key', 'sk-proj-...');
```

4. Обнови страницу (F5)

✅ **Плюс:** Ключ хранится только в браузере, не виден в коде.

⚠️ **Минус:** При очистке данных браузера нужно ввести заново.

---

## Часть 3: Настройка бота для передачи изображений

Чтобы бот мог отправлять изображения в мини-приложение, нужно настроить его код.

### Вариант A: Передача URL изображения (рекомендуется)

В коде бота, когда пользователь отправляет фото:

```python
# Пример на Python (python-telegram-bot)
from telegram import Update
from telegram.ext import ContextTypes

async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    photo = update.message.photo[-1]  # Берем самое большое фото
    file = await context.bot.get_file(photo.file_id)
    
    # Получаем URL файла
    file_url = f"https://api.telegram.org/file/bot{context.bot.token}/{file.file_path}"
    
    # Открываем Mini App с start_param
    web_app_url = "https://your-app.netlify.app"
    start_param = f"img_url_{file_url.replace('https://', '').replace('http://', '')}"
    # Или используй encodeURIComponent в JavaScript
    
    await update.message.reply_text(
        "Анализирую изображение...",
        reply_markup=InlineKeyboardMarkup([[
            InlineKeyboardButton(
                "Открыть приложение",
                web_app=WebAppInfo(url=f"{web_app_url}?startapp={start_param}")
            )
        ]])
    )
```

### Вариант B: Передача file_id (требует bot_token)

1. В приложении добавь токен бота:
   ```javascript
   sessionStorage.setItem('bot_token', 'YOUR_BOT_TOKEN');
   ```
2. В коде бота передавай `file_id`:
   ```python
   start_param = f"img_{photo.file_id}"
   ```

---

## Часть 4: Проверка работы

1. Открой приложение в Telegram
2. Отправь боту фотографию товара (например, скриншот с сайта магазина)
3. Бот должен открыть мини-приложение
4. Должно появиться сообщение "Анализирую изображение..."
5. Через несколько секунд откроется форма с автозаполненными данными
6. Проверь, что поля заполнились правильно
7. При необходимости исправь данные
8. Заполни комментарий, выбери категорию и вишлист
9. Сохрани карточку

---

## Часть 5: Устранение проблем

### Ошибка: "Не указан API ключ"

**Решение:** Проверь, что хотя бы один ключ указан:
- **Gemini:** `const GEMINI_API_KEY = '...'` или `sessionStorage.getItem('gemini_key')`
- **OCRAPI:** `const OCRAPI_KEY = '...'` или `sessionStorage.getItem('ocrapi_key')`
- **OpenAI:** `const OPENAI_API_KEY = '...'` или `sessionStorage.getItem('openai_key')`

Рекомендуется начать с **Gemini** — он бесплатный и работает лучше всего!

---

### Ошибка: "Не удалось проанализировать изображение"

**Возможные причины:**
1. Неверный API ключ
2. Изображение слишком большое или повреждено
3. Проблемы с сетью
4. Для OpenAI: недостаточно средств на балансе

**Решение:**
- Проверь правильность ключа
- Попробуй другое изображение
- Если используешь OpenAI — проверь баланс на https://platform.openai.com/usage
- Попробуй другой API (например, Gemini вместо OpenAI)

---

### Ошибка: "Не удалось получить изображение от бота"

**Решение:**
- Если используешь `img_url_`: проверь, что URL правильный и доступен
- Если используешь `img_<file_id>`: убедись, что `bot_token` указан в sessionStorage

---

### Изображение анализируется, но данные неверные

**Это нормально!** AI не всегда распознаёт всё идеально.

**Что делать:**
- Проверь и исправь данные в форме перед сохранением
- Улучши качество фотографии (лучше освещение, чёткий текст)
- Обрежь фото, чтобы был виден только товар и цена

---

## Часть 6: Безопасность и расходы

### Сравнение вариантов

| API | Стоимость | Качество | Скорость | Доступность |
|-----|-----------|----------|----------|-------------|
| **Tesseract.js** | 🆓 Бесплатно | ⭐⭐⭐ Хорошее | 10-20 сек | ✅ Везде (Беларусь/Россия) |
| **Gemini** | 🆓 Бесплатно | ⭐⭐⭐⭐⭐ Отличное | 2-5 сек | ⚠️ Может быть заблокирован |
| **OCRAPI** | 🆓 Бесплатно | ⭐⭐⭐ Хорошее | 3-7 сек | ✅ Обычно доступен |
| **OpenAI** | 💰 ~$0.01-0.03/запрос | ⭐⭐⭐⭐⭐ Отличное | 2-5 сек | ⚠️ Может быть заблокирован |

**Рекомендация для Беларуси/России:** Используй **Tesseract.js** — он работает везде и не требует ключей!

### Как контролировать расходы (только для OpenAI)

1. Зайди на https://platform.openai.com/usage
2. Там видно:
   - Сколько потрачено за день/месяц
   - Сколько запросов сделано
   - Можно установить лимит расходов

### Рекомендации по безопасности

1. **Не публикуй API ключ** в открытых репозиториях GitHub
2. Для продакшена используй **серверный прокси**:
   - Создай серверный endpoint (например, на Vercel Functions)
   - Храни ключ в переменных окружения сервера
   - Мини-приложение отправляет изображение на сервер
   - Сервер анализирует и возвращает результат
3. Установи **лимит расходов** в настройках OpenAI

---

## Готово!

Теперь функция анализа изображений должна работать. Если возникнут вопросы — пиши!
